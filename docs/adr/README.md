# ADR Log

Dieses Verzeichnis enthält kurze, stabile Architekturentscheidungen
(Architecture Decision Records) für ADR – Agent-Oriented Declarative Runtime.

Längere Diskussionen und Reviews liegen in `docs/dialog/`.
Die ADR-Dateien fassen die endgültigen Entscheidungen zusammen.